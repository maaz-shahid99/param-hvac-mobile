/// App version + build tag shown in Settings.
///
/// BUMP [kAppBuild] on every app change so you can confirm at a glance which
/// build is actually installed on the phone (e.g. when debugging "am I running
/// the latest code?"). Keep it short and increment the trailing number.
const String kAppVersion = '1.0.0';
const String kAppBuild = 'b10 · 2026-06-05 · cloud device roster + faster offline (30s)';
